# Copy to .env for local builds if needed (never commit .env).
# Primary content path is JSON under src/content/docs/ (Apps Script publisher).
# These pull-API vars are optional and usually unused under Varsity Workspace:

# DOC_CONTENT_API_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
# DOC_CONTENT_API_SECRET=optional
